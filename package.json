{
  "name": "boss-media-www",
  "version": "0.0.1",
  "main": "dist",
  "scripts": {
    "update": "npx ncu -u -a",
    "lint": "eslint src --fix",
    "test": "mocha --options .mocha.opts",
    "test:dev": "npx watch 'npm run test' src",
    "build": "rm -rf dist; mkdir dist; babel src --out-dir dist --copy-files",
    "build:dev": "npm run build -- --watch",
    "webpack": "NODE_ENV=production webpack",
    "webpack:dev": "NODE_ENV=development webpack-dev-server",
    "serve": "node ./dist/scripts/serve.js",
    "serve:dev": "NODE_ENV=development nodemon --watch dist/api ./dist/scripts/serve.js",
    "start": "npm i && npm run lint && npm run test && npm run build && npm run webpack && npm run serve"
  },
  "keywords": [],
  "author": "BenZed",
  "license": "ISC",
  "description": "generated by BenZed's create-project dev tool",
  "dependencies": {
    "@benzed/react": "^0.0.6",
    "@benzed/app": "^0.1.8",
    "styled-components": "^3.2.6",
    "react": "^16.3.2",
    "react-router-dom": "^4.2.2",
    "react-dom": "^16.3.2"
  },
  "devDependencies": {
    "@benzed/dev": "^0.4.0"
  }
}
